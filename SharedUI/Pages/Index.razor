@page "/"
@inject NavigationManager Navigation
<GpsTracker />
@code {
    protected override void OnInitialized()
    {
        // Redirect root to admin login (soft redirect, no forceLoad for MAUI WebView compatibility)
        Navigation.NavigateTo("/admin/login");
    }
}
