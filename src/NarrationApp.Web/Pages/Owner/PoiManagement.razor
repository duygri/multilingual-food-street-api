@page "/owner/poi-management"
@attribute [Authorize(Roles = "poi_owner")]
@inject NavigationManager NavigationManager

<PageTitle>Chuyển sang danh sách POI</PageTitle>

<section class="panel-shell owner-poi-management-redirect">
    <p class="panel-shell__eyebrow">POI</p>
    <h2>Đang chuyển sang danh sách POI</h2>
    <p class="panel-shell__copy">
        Màn quản lý POI cũ đã được tách thành danh sách, tạo mới và chi tiết. Nếu trình duyệt không tự chuyển,
        hãy mở <a href="/owner/pois">/owner/pois</a>.
    </p>
</section>
