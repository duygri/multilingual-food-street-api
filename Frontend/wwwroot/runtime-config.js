window.FoodStreetMapConfig = Object.assign(window.FoodStreetMapConfig || {}, {
    // Browser key for Google Maps JavaScript / Places / Static Maps.
    // Restrict this key by HTTP referrer before putting the real value here.
    googleMapsApiKey: "YOUR_FRONTEND_API_KEY_HERE"
});
